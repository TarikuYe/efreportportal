'use client'

// Vercel Analytics is only active when deployed to Vercel.
// This stub prevents the package from injecting a script tag in local dev
// (which would cause a 404 for /_vercel/insights/script.js).
export function VercelAnalytics() {
  return null
}
